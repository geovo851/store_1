$('tbody#products').html('<%= escape_javascript render(partial: "product", :collection => @products, :as => :product) %>');

$('#paginator').html('<%= escape_javascript(paginate(@products, :remote => true).to_s) %>');
