<!DOCTYPE html>
<html>
  <head>
  <title><%= yield(:title)%> | Electronics store</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>

    <%= render 'shared/navbar' %>

    <div class='main-page-content <%= controller.controller_name %>'>
      <div class="container">
        <% flash.each do |key, value| %>
          <%= content_tag(:div, value, class: "alert #{bootstrap_class_for(key)} flash") %>
        <% end %>

        <%= yield %>
      </div>
    </div>

    <footer>
      <p>&copy; Electronics store 2000-2015. All Rights Reserved. </p>
    </footer>

  </body>
</html>
